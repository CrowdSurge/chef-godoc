server {
    listen 80;
    server_name <%= @host %>;

    rewrite ^/$ /pkg last;

    location / {
        proxy_pass       http://localhost:9200;
        proxy_set_header Host      $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

}
